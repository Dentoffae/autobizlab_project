# Доступ к /pgadmin/ только с перечисленных IP (офис / VPN / дом).
# Добавьте свои строки allow при необходимости; без совпадения — 403.

# Частый клиент из access-логов (основной доступ к pgAdmin):
allow 5.193.57.236;
# Публичный IPv4 этого сервера (curl/проверки с хоста):
allow 85.198.66.152;
deny all;
