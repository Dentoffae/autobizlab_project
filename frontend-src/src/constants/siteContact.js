/** Публичный контактный email: футер, политика, JSON-LD (должен совпадать с index.html при правке). */
export const SITE_CONTACT_EMAIL = 'logosconsultlab@gmail.com'

/** Базовый origin (HTTPS, без слэша) — canonical, OG, hreflang. */
export const SITE_ORIGIN = 'https://autobizlab.store'
